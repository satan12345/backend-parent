<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mooc.meeting.film.backendfilm.dao.MoocFilmInfoTDao">

    <resultMap type="com.mooc.meeting.film.backendfilm.entity.MoocFilmInfoT" id="MoocFilmInfoTMap">
        <result property="uuid" column="UUID" jdbcType="INTEGER"/>
        <result property="filmId" column="film_id" jdbcType="VARCHAR"/>
        <result property="filmEnName" column="film_en_name" jdbcType="VARCHAR"/>
        <result property="filmScore" column="film_score" jdbcType="VARCHAR"/>
        <result property="filmScoreNum" column="film_score_num" jdbcType="INTEGER"/>
        <result property="filmLength" column="film_length" jdbcType="INTEGER"/>
        <result property="biography" column="biography" jdbcType="VARCHAR"/>
        <result property="directorId" column="director_id" jdbcType="INTEGER"/>
        <result property="filmImgs" column="film_imgs" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MoocFilmInfoTMap">
        select
          UUID, film_id, film_en_name, film_score, film_score_num, film_length, biography, director_id, film_imgs
        from mooc_meetingfilm.mooc_film_info_t
        where UUID = #{uuid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MoocFilmInfoTMap">
        select
          UUID, film_id, film_en_name, film_score, film_score_num, film_length, biography, director_id, film_imgs
        from mooc_meetingfilm.mooc_film_info_t
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MoocFilmInfoTMap">
        select
          UUID, film_id, film_en_name, film_score, film_score_num, film_length, biography, director_id, film_imgs
        from mooc_meetingfilm.mooc_film_info_t
        <where>
            <if test="uuid != null">
                and UUID = #{uuid}
            </if>
            <if test="filmId != null and filmId != ''">
                and film_id = #{filmId}
            </if>
            <if test="filmEnName != null and filmEnName != ''">
                and film_en_name = #{filmEnName}
            </if>
            <if test="filmScore != null and filmScore != ''">
                and film_score = #{filmScore}
            </if>
            <if test="filmScoreNum != null">
                and film_score_num = #{filmScoreNum}
            </if>
            <if test="filmLength != null">
                and film_length = #{filmLength}
            </if>
            <if test="biography != null and biography != ''">
                and biography = #{biography}
            </if>
            <if test="directorId != null">
                and director_id = #{directorId}
            </if>
            <if test="filmImgs != null and filmImgs != ''">
                and film_imgs = #{filmImgs}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="uuid" useGeneratedKeys="true">
        insert into mooc_meetingfilm.mooc_film_info_t(film_id, film_en_name, film_score, film_score_num, film_length, biography, director_id, film_imgs)
        values (#{filmId}, #{filmEnName}, #{filmScore}, #{filmScoreNum}, #{filmLength}, #{biography}, #{directorId}, #{filmImgs})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update mooc_meetingfilm.mooc_film_info_t
        <set>
            <if test="filmId != null and filmId != ''">
                film_id = #{filmId},
            </if>
            <if test="filmEnName != null and filmEnName != ''">
                film_en_name = #{filmEnName},
            </if>
            <if test="filmScore != null and filmScore != ''">
                film_score = #{filmScore},
            </if>
            <if test="filmScoreNum != null">
                film_score_num = #{filmScoreNum},
            </if>
            <if test="filmLength != null">
                film_length = #{filmLength},
            </if>
            <if test="biography != null and biography != ''">
                biography = #{biography},
            </if>
            <if test="directorId != null">
                director_id = #{directorId},
            </if>
            <if test="filmImgs != null and filmImgs != ''">
                film_imgs = #{filmImgs},
            </if>
        </set>
        where UUID = #{uuid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from mooc_meetingfilm.mooc_film_info_t where UUID = #{uuid}
    </delete>

</mapper>