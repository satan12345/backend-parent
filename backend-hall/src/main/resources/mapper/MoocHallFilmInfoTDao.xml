<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mooc.meeting.film.hall.dao.MoocHallFilmInfoTDao">

    <resultMap type="com.mooc.meeting.film.hall.entity.MoocHallFilmInfoT" id="MoocHallFilmInfoTMap">
        <result property="uuid" column="UUID" jdbcType="INTEGER"/>
        <result property="filmId" column="film_id" jdbcType="INTEGER"/>
        <result property="filmName" column="film_name" jdbcType="VARCHAR"/>
        <result property="filmLength" column="film_length" jdbcType="VARCHAR"/>
        <result property="filmCats" column="film_cats" jdbcType="VARCHAR"/>
        <result property="filmLanguage" column="film_language" jdbcType="VARCHAR"/>
        <result property="actors" column="actors" jdbcType="VARCHAR"/>
        <result property="imgAddress" column="img_address" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MoocHallFilmInfoTMap">
        select
          UUID, film_id, film_name, film_length, film_cats, film_language, actors, img_address
        from mooc_meetingfilm.mooc_hall_film_info_t
        where UUID = #{uuid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MoocHallFilmInfoTMap">
        select
          UUID, film_id, film_name, film_length, film_cats, film_language, actors, img_address
        from mooc_meetingfilm.mooc_hall_film_info_t
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MoocHallFilmInfoTMap">
        select
          UUID, film_id, film_name, film_length, film_cats, film_language, actors, img_address
        from mooc_meetingfilm.mooc_hall_film_info_t
        <where>
            <if test="uuid != null">
                and UUID = #{uuid}
            </if>
            <if test="filmId != null">
                and film_id = #{filmId}
            </if>
            <if test="filmName != null and filmName != ''">
                and film_name = #{filmName}
            </if>
            <if test="filmLength != null and filmLength != ''">
                and film_length = #{filmLength}
            </if>
            <if test="filmCats != null and filmCats != ''">
                and film_cats = #{filmCats}
            </if>
            <if test="filmLanguage != null and filmLanguage != ''">
                and film_language = #{filmLanguage}
            </if>
            <if test="actors != null and actors != ''">
                and actors = #{actors}
            </if>
            <if test="imgAddress != null and imgAddress != ''">
                and img_address = #{imgAddress}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="uuid" useGeneratedKeys="true">
        insert into mooc_meetingfilm.mooc_hall_film_info_t(film_id, film_name, film_length, film_cats, film_language, actors, img_address)
        values (#{filmId}, #{filmName}, #{filmLength}, #{filmCats}, #{filmLanguage}, #{actors}, #{imgAddress})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update mooc_meetingfilm.mooc_hall_film_info_t
        <set>
            <if test="filmId != null">
                film_id = #{filmId},
            </if>
            <if test="filmName != null and filmName != ''">
                film_name = #{filmName},
            </if>
            <if test="filmLength != null and filmLength != ''">
                film_length = #{filmLength},
            </if>
            <if test="filmCats != null and filmCats != ''">
                film_cats = #{filmCats},
            </if>
            <if test="filmLanguage != null and filmLanguage != ''">
                film_language = #{filmLanguage},
            </if>
            <if test="actors != null and actors != ''">
                actors = #{actors},
            </if>
            <if test="imgAddress != null and imgAddress != ''">
                img_address = #{imgAddress},
            </if>
        </set>
        where UUID = #{uuid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from mooc_meetingfilm.mooc_hall_film_info_t where UUID = #{uuid}
    </delete>

</mapper>